apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: airflow-dags
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/bladezx/k3s-airflow-dags.git
  ref:
    branch: main
  secretRef:
    name: flux-system