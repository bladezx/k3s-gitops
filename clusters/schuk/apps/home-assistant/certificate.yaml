apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-homeassistant
  namespace: home-assistant
spec:
  secretName: tls-homeassistant
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - homeassistant.schuk.local
